<p>Log In or Sign Up as local business or local non-profit to create, edit or delete a project.</p><br>

<% if logged_in? %>
    <div class="btn-group">
        <button onclick="document.location='/'">Go to Profile</button>
        <button onclick="document.location='/logout'">Log Out</button>
    </div>
<% end %>

<h2>All Projects</h2>

<% @projects.each do |project| %>
    <a href="/users/<%= project.user.id %>"><strong><%= project.user.name %></a><br>
    <a href="/projects/<%= project.id %>"><strong>Position: </strong><%= project.title %></a><br>
    <img src="<%= project.image_url %>" height="150"><br>
    <strong>Information: </strong><%= project.information %><br><br>
<% end %>